[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "audit"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.12.0",
    "httpx>=0.27.0",
    "starlette>=0.41.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/audit"]
